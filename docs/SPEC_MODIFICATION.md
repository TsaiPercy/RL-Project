# Spec Modification Log

| # | Date | Section | Change Summary | Trigger | Type × Scope |
|---|------|---------|----------------|---------|---------------|
| 1 | 2026-05-01 | §5.4 Agent Pool | Added §5.4.1 Toy Case Agent Pool (SB3 PPO on DoorKeyEnv) | user request | Add × Cascading |
| 2 | 2026-05-01 | §5.5 階段計畫 | Inserted Phase 0 (Toy Case) before Phase 1; updated ordering description | user request | Add × Cascading |
| 3 | 2026-05-01 | §9 Datasets | Added Toy Case Agent Pool subsection under Agent Pool | user request | Add × Cascading |
| 4 | 2026-05-01 | §10 Experiment Design | Added Experiment T: Toy Case Pipeline Smoke Test; updated Experiment Ordering | user request | Add × Cascading |
| 5 | 2026-05-01 | §11.4 Pseudo Config | Added toy_case config block (train_env, agent steps, PPO hyperparams) | user request | Add × Cascading |
| 6 | 2026-05-01 | §12 Experiment Impl | Added Experiment T implementation architecture (4-step flow) | user request | Add × Cascading |
| 7 | 2026-05-01 | §16 Directory Structure | Added toy_case/ directory (train_agent.py, run_toy_pipeline.py) | user request | Add × Cascading |
| 8 | 2026-05-01 | §11.2 Module A | `get_ref_log_probs` 簽名新增 `prompt_ids` 參數 | impl-updated (MA-1) | L1 |
| 9 | 2026-05-01 | §16 Directory Structure, §11.4 Pseudo Config | 新增 `shared/env_setup.py`、`cache/`、`setup_env.sh` 至目錄；新增 Entry Point Convention（所有 entry point 頂端需呼叫 `setup_project_cache()`）；§11.4 加入 `cache_dir` 配置項 | user request | Add × Localized |
| 10 | 2026-05-01 | §5.4 Agent Pool | Agent 從 BabyAI pretrained 改為自行訓練 curriculum（BabyAI 環境 room_size=15，8 關由簡到難），強弱差異透過成功率門檻與訓練環境數量製造 | user request | Modify × Cascading |
| 11 | 2026-05-01 | §5.4.1, §9 Toy Case | DoorKeyEnv size=13 → room_size=15（統一為 15×15 total, 13×13 usable） | user request | Modify × Cascading |
| 12 | 2026-05-01 | §11.4 Pseudo Config | 新增 agent_training config block（curriculum 環境列表、成功率門檻、strong/weak 配置） | user request | Add × Cascading |
| 13 | 2026-05-01 | §16 Directory Structure | 新增 `agent_training/` 目錄（train_curriculum.py, evaluate_agent.py） | user request | Add × Localized |
| 14 | 2026-05-02 | §5.4, §11.4 | 將 8-task 多任務 curriculum 換為 9-env GoTo 家族單任務 curriculum；新增 per-env base threshold（0.90→0.60）；將 `success_threshold_override` 改為 `success_increase`（加法 delta）；將 `total_timesteps` 改為 `max_timesteps`；`curriculum_levels` 8→9 | user request | Modify × Cascading |
| 15 | 2026-05-02 | §11.4 Pseudo Config | 新增 `eval_episodes: 50` 和 `eval_chunk_steps: 100_000` 至 `agent_training` block — 控制 curriculum 訓練中每次 success-rate 評估的 episode 數與訓練 chunk 大小 | impl-updated (AT-2) | L1 |
| 16 | 2026-05-02 | §11.4 Pseudo Config | 新增 `cnn_features_dim: 256` 至 `agent_training.ppo_hyperparams`；新增 `BabyAIFeaturesExtractor`（kernel_size=2 小型 CNN）以替換 SB3 NatureCNN（8×8 kernel 不相容於 7×7 obs，導致 RuntimeError） | impl-updated (AT-4) | L1 |
| 17 | 2026-05-02 | §5.4, §11.4 | Curriculum 各關卡改用 BabyAI 預設環境大小，不再統一覆寫 `room_size=15`；移除 `agent_training.room_size` config 欄位；移除 `make_env_fn` 與 `evaluate_agent` 的 `room_size` 參數及 try/except fallback；toy_case 的 DoorKey 15×15 不受影響（env name 已固定） | user request | L2 |
| 18 | 2026-05-03 | §5.4, §11.4, §16 | Phase A 觀察值升級：ImgObsWrapper → MissionTokenizer (dict obs `{image, direction, mission}`)；`PPO("CnnPolicy")` → `PPO("MultiInputPolicy")` + `BabyAIDictExtractor` (CNN + mission text mean-pool + direction embedding 融合)；新增 config 欄位 `mission_max_len`/`vocab_size`/`text_embed_dim`/`dir_embed_dim`；`ppo_hyperparams.cnn_features_dim` 改名為 `features_dim`；新增 `agent_training/wrappers.py` 與 `agent_training/extractors.py`。修正 BabyAI-GoTo-v0 mission-blindness。LSTM/RecurrentPPO 暫不導入（Phase B deferred） | user request | L2 |
| 19 | 2026-05-03 | §11.2 Module B | `batch_evaluate()` 新增 optional `agent_ids` keyword argument，讓呼叫方指定使用哪組 agent pool（Per SPEC §8 quick/full mode） | impl-updated (MC-4) | L2 |
| 20 | 2026-05-07 | §11.2 Module A | LLMPolicy 四項記憶體優化（4090 OOM fix）：(1) `__init__` 新增 `gradient_checkpointing=True` flag，啟用 `enable_input_require_grads()` 配合 PEFT；(2) 移除獨立 reference model 載入，`get_ref_log_probs` 改用 `model.disable_adapter()` context manager 共用 base；(3) `_compute_log_probs`/`_compute_current_log_probs` 用 logsumexp 取代 log_softmax，避免 (B, seq, vocab=152K) softmax 副本；(4) 新增 `use_flash_attention=True` flag，缺 flash-attn 套件時 fallback 到 SDPA。估計 peak memory 從 ~56GB 降到 ~12GB（B=2, seq≈2200）。`self.ref_model` 保留為 None（backward compat） | impl-updated (MA-MEM-*) | L2 |
| 21 | 2026-05-07 | §11.2 Module A | `generate()` / `generate_with_chat_template()` 新增 `compute_log_probs: bool = True` 參數；False 時跳過冗餘的 `_compute_log_probs` 全序列 forward（省 ~6.6GB at B=10）。同時 (1) `output_scores` 從強制 True 改為固定 False（從未被使用，省 ~6.2GB 累積暫態）；(2) generation 期間 try/finally 暫時設 `use_cache=True` 並還原（訓練用 False 是 KV cache vs. grad 衝突；推理沿用會災難）。Sanity check OOM fix | impl-updated (MA-MEM-6) | L1 |
| 22 | 2026-05-07 | §11.2 Module A, §11.4 Pseudo Config | `update()` 新增 `micro_batch_size: Optional[int] = None` 參數，支援梯度累積：當給定時把 `grpo_batch` 沿 dim0 切 `ceil(B / micro)` 個 chunk，逐個 forward+backward（loss 乘 `chunk_size / actual_b` 加權），累積後做 1 次 `clip_grad_norm_` + `optimizer.step()`；數學等價於 mean over B 但 peak memory 由 micro_batch_size 決定。`config/default.yaml` 對應移除 `gradient_accumulation_steps`（GRPO effective_batch=batch_size×group_size 已固定，accumulation 是冗餘變數），加入 `micro_batch_size: 1`。新增 `toy_case/train_smoke_test.py` 驗證訓練 path 記憶體不爆 | impl-updated (MA-MEM-5) | L2 |
| 23 | 2026-05-07 | §11 Module A prompts, §12 Experiment 0 | (1) `MINIGRID_SYSTEM_PROMPT` 結尾加 `/no_think` 後綴關閉 Qwen3 reasoning mode：reward 訊號（agent regret）不獎勵推理品質，但 thinking 會吃 ~90% 的 max_new_tokens 配額且降低 GRPO group sampling 多樣性；(2) `toy_case/sanity_check.py:simple_parse()` 強化 — 先用 `text.rfind("</think>")` 砍除 reasoning 區，並把 `text.find("Grid:")` 改為 `rfind` 抓最後一次出現，避免推理草稿干擾。診斷自 sanity_check parse rate 僅 10%（10 張 1 張過），其中 6 張是 parser 抓到推理區草稿 | impl-updated + diagnostic | L1 |
| 24 | 2026-05-07 | §11.2 Module A, §11.4 Pseudo Config, prompts | Qwen3.5 thinking mode 控制路徑修正 — Amendment #23 加的 `/no_think` 在 Qwen3.5 無效（官方 model card：「Unlike Qwen3, Qwen3.5 does not support /think or /nothink directives」）。改採官方支援的 `tokenizer.apply_chat_template(..., enable_thinking=False)`：`LLMPolicy.__init__` 新增 `enable_thinking: bool = False` 參數；`generate_with_chat_template` 透傳到 `apply_chat_template`；同時把 Qwen3.5 non-thinking 官方建議採樣參數收進 ctor + config（`top_p: 0.8` 取代 hardcoded 0.95、`top_k: 20`、`presence_penalty: 1.5`、`temperature: 0.7`）。`MINIGRID_SYSTEM_PROMPT` 移除 `/no_think` 後綴。Amendment #23 的 parser 防呆（`</think>` 砍除 + `rfind("Grid:")`）保留作為防呆 | impl-updated + bugfix | L1 |
| 25 | 2026-05-07 | §3 Modality, §11.2 Module B, §11.4 Pseudo Config | LLM 輸出 grid 格式從「13×13 inner area，外牆由 engine 補」改為「**完整 15×15 含外牆 ring**，LLM 自生外牆」。Object/agent_start 座標範圍對應從 `[0, 12]` 改為 `[1, 13]`（避開外牆 col/row 0 與 14）。Parser 加「外牆 ring 完整性」驗證 — 檢查 row 0/14 全為 'W'、每列 col 0/14 為 'W'。改動檔案：`llm_policy/prompts.py`（rules + example）、`toy_case/sanity_check.py:simple_parse()`、`config/default.yaml:grid_size 13→15`、`toy_case/run_toy_pipeline.py` mock level、`shared/types.py` + `shared/type_examples.py` 全部範例、`docs/SPEC.md` §3 + §11、`CLAUDE.md` Key Data Contracts、`toy_case/TOY_CASE.md`。MiniGrid env 本身不變（一直就是 15×15 with outer walls） | user request | Modify × Cascading |
